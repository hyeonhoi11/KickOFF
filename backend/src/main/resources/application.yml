spring:
  application:
    name: kickoff
  datasource:
    url: jdbc:postgresql://localhost:5432/kickoff
    username: kickoff
    password: kickoff1234
    driver-class-name: org.postgresql.Driver
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
    show-sql: true
  data:
    redis:
      host: localhost
      port: 6379

server:
  port: 8080

jwt:
  secret: kickoff-secret-key-for-jwt-token-signing-must-be-long-enough
  access-token-expiration: 3600000
  refresh-token-expiration: 1209600000