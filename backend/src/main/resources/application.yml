spring:
  application:
    name: kickoff
  datasource:
    url: jdbc:postgresql://localhost:5432/kickoff
    username: kickoff
    password: kickoff1234
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8080